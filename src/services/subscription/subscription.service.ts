import { ISubscriptionDocument } from "../../models/subscription/subscription.model";
import { ICreateSubscriptionParams } from "./subscription-service.type";

const createSubscription = async (params: ICreateSubscriptionParams) => {};

const getSubscriptions = async (filters) => {};

export default { createSubscription, getSubscriptions };
